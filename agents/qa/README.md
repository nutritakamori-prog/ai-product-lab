# QA agents

Empty — QA Investigator and Visual QA are not built yet (Phase 5). Add an
agent here as a `.ts` file exporting a default `AgentDefinition` (see
`agents/system/agent-protocol.ts`), then list it in `agents/index.ts`.
