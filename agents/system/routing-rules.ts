/**
 * Placeholder for the Smart Router (Phase 4) — routing logic does not exist
 * yet, on purpose (this project's own build rules: don't build ahead of the
 * phase that needs it). This file exists now only so the category an agent
 * declares (`agent-protocol.ts`) has one documented future home for the
 * rules that will decide, per task, which categories/agents to call.
 *
 * Re-exported, not duplicated — AGENT_CATEGORIES is defined once in
 * agent-protocol.ts.
 */
export { AGENT_CATEGORIES, type AgentCategory } from "./agent-protocol";
