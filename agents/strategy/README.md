# Strategy agents

Empty — Product Strategist and CEO/Head of Product are not built yet
(Phase 12). Add an agent here as a `.ts` file exporting a default
`AgentDefinition` (see `agents/system/agent-protocol.ts`), then list it
in `agents/index.ts`.
