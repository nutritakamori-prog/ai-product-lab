# Orchestration agents

Empty. This category exists for future agents that are themselves part of
orchestrating other agents (distinct from `src/core/orchestrator`, which is
the Master Orchestrator/Smart Router *code*, not an agent). Nothing here
until there's a concrete need — see `docs/DECISIONS.md`.
